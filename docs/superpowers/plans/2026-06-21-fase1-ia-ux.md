# Fase 1 — IA + base UX — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Limpiar IA del menú (eliminar `/kpis`, `/sales`, `/import`; añadir Suscripciones; quitar código muerto de ventas) y unificar controles repetidos de los informes en piezas reutilizables.

**Architecture:** Componentes presentacionales pequeños en `src/components/common/`; borrado de rutas; refactor 1:1 (sin cambiar estado/lógica) de las páginas de informe. Validación con tsc + lint + build (no hay tests unitarios nuevos).

**Tech Stack:** Next 16 (App Router), React 19, Tailwind, UI propios, lucide-react.

---

### Task 1: Piezas de control reutilizables

**Files:** Create `src/components/common/record-type-select.tsx`, `search-input.tsx`, `csv-button.tsx`, `empty-state.tsx`

- [ ] **Step 1: Crear los 4 componentes**

`src/components/common/record-type-select.tsx`:

```tsx
"use client";

import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";

export function RecordTypeSelect({
  value,
  onValueChange,
  className = "w-[200px]",
}: {
  value: "SALES_ORDER" | "INVOICE";
  onValueChange: (v: "SALES_ORDER" | "INVOICE") => void;
  className?: string;
}) {
  return (
    <Select value={value} onValueChange={(v) => { if (v) onValueChange(v as "SALES_ORDER" | "INVOICE"); }}>
      <SelectTrigger className={className}>
        <SelectValue>{value === "SALES_ORDER" ? "Órdenes de Venta (OV)" : "Facturas (FAC)"}</SelectValue>
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="INVOICE">Facturas (FAC)</SelectItem>
        <SelectItem value="SALES_ORDER">Órdenes de Venta (OV)</SelectItem>
      </SelectContent>
    </Select>
  );
}
```

`src/components/common/search-input.tsx`:

```tsx
"use client";

import { Search } from "lucide-react";
import { Input } from "@/components/ui/input";
import { cn } from "@/lib/utils";

export function SearchInput({
  value,
  onChange,
  placeholder = "Buscar…",
  className,
}: {
  value: string;
  onChange: (v: string) => void;
  placeholder?: string;
  className?: string;
}) {
  return (
    <div className={cn("relative flex-1 min-w-[260px]", className)}>
      <Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />
      <Input placeholder={placeholder} className="pl-10" value={value} onChange={(e) => onChange(e.target.value)} />
    </div>
  );
}
```

`src/components/common/csv-button.tsx`:

```tsx
"use client";

import { Download } from "lucide-react";
import { Button } from "@/components/ui/button";

export function CsvButton({ onClick, disabled }: { onClick: () => void; disabled?: boolean }) {
  return (
    <Button variant="outline" size="sm" onClick={onClick} disabled={disabled}>
      <Download className="mr-2 h-4 w-4" /> CSV
    </Button>
  );
}
```

`src/components/common/empty-state.tsx`:

```tsx
export function EmptyState({ message }: { message: string }) {
  return <div className="text-center text-muted-foreground py-10">{message}</div>;
}
```

- [ ] **Step 2:** `npx tsc --noEmit && npm run lint` → tsc rc 0; lint 0 errores.
- [ ] **Step 3:** Commit:

```bash
git add src/components/common/
git commit -m "feat(ux): piezas de control reutilizables (RecordTypeSelect/SearchInput/CsvButton/EmptyState)"
```

---

### Task 2: IA — eliminar rutas + menú Suscripciones

**Files:** Delete dirs `src/app/(dashboard)/kpis`, `/sales`, `/import`; Modify `src/components/layout/sidebar.tsx`, `mobile-nav.tsx`

- [ ] **Step 1: Borrar las 3 rutas**

```bash
rm -rf "src/app/(dashboard)/kpis" "src/app/(dashboard)/sales" "src/app/(dashboard)/import"
```

- [ ] **Step 2: Añadir Suscripciones al sidebar**

Lee `src/components/layout/sidebar.tsx`. Añade `CreditCard` al import de `lucide-react` y este ítem al array `navItems` (después de `/categories`):

```tsx
  { href: "/subscriptions", label: "Suscripciones", icon: CreditCard },
```

- [ ] **Step 3: Añadir Suscripciones al mobile-nav**

Lee `src/components/layout/mobile-nav.tsx`. Añade `CreditCard` al import de `lucide-react` y el mismo ítem tras `/categories`:

```tsx
  { href: "/subscriptions", label: "Suscripciones", icon: CreditCard },
```

- [ ] **Step 4:** `npx tsc --noEmit && npm run lint` → tsc rc 0; lint 0 errores. (Si lint marca algún import sin usar por las rutas borradas, lo arreglas.)
- [ ] **Step 5:** Commit:

```bash
git add -A
git commit -m "feat(ia): eliminar /kpis /sales /import; añadir Suscripciones al menú"
```

---

### Task 3: Quitar código muerto de mutación de ventas

**Files:** Modify `src/actions/sales-actions.ts`

Tras borrar `/sales` y `/import`, estas funciones quedan sin callers.

- [ ] **Step 1: Eliminar funciones muertas**

En `src/actions/sales-actions.ts`, elimina por completo: `upsertSalesRecord`, `bulkUpsertSalesRecords`, `deleteSalesRecord`, y la constante `SALES_READONLY_MSG` (y su comentario). **Conserva** `getSalesData`, `getMonthlyTotals`, `getAnnualByCategory`, `warnIfLargeSalesPayload` y el const `SALES_PAYLOAD_WARN_THRESHOLD`.

- [ ] **Step 2: Limpiar imports sin usar**

Ejecuta `npm run lint` y elimina del `import` los símbolos que queden sin usar (probables: `requireRole`, `uuidSchema`, `salesRecordSchema`, `sql`, `z`). Conserva los que sigan en uso (`requireApproved`, `eq`, `and`, `desc`, `asc`, `db`, `salesRecords`, `categories`, `hubEnabled`, `getHubSalesRows`, `logWarn`, tipos). Repite hasta `npm run lint` = 0 errores.

- [ ] **Step 3:** `npx tsc --noEmit && npm run lint && npx vitest run` → tsc 0; lint 0 errores; tests verdes.
- [ ] **Step 4:** Commit:

```bash
git add src/actions/sales-actions.ts
git commit -m "chore(ventas): eliminar mutaciones sin uso (read-only desde hub)"
```

---

### Task 4: Aplicar piezas en `articulos/page.tsx`

**Files:** Modify `src/app/(dashboard)/articulos/page.tsx`

- [ ] **Step 1: Importar las piezas**

Añade:

```tsx
import { RecordTypeSelect } from "@/components/common/record-type-select";
import { SearchInput } from "@/components/common/search-input";
import { CsvButton } from "@/components/common/csv-button";
import { EmptyState } from "@/components/common/empty-state";
```

- [ ] **Step 2: Sustituir los controles (lee el archivo y reemplaza 1:1)**

- El `<Select>` OV/FAC (con `SelectTrigger w-[200px]` y los dos `SelectItem`) → `<RecordTypeSelect value={tipo} onValueChange={setTipo} />`.
- El `<div className="relative flex-1 min-w-[260px]">…<Search/>…<Input/></div>` de búsqueda → `<SearchInput value={search} onChange={setSearch} placeholder="Buscar por SKU o nombre…" />`.
- El `<Button variant="outline" size="sm" onClick={exportCsv} disabled={view.length === 0}>…CSV</Button>` → `<CsvButton onClick={exportCsv} disabled={view.length === 0} />`.
- En la celda vacía `<TableCell colSpan={6} className="text-center text-muted-foreground py-10">Sin resultados…</TableCell>` → `<TableCell colSpan={6}><EmptyState message="Sin resultados para el rango/criterios seleccionados." /></TableCell>`.

- [ ] **Step 3: Quitar imports que queden sin usar** (`Search`, `Download`, y `Select*`/`Input`/`Button` si ya no se usan en el archivo). `npm run lint` debe quedar a 0 errores.
- [ ] **Step 4:** `npx tsc --noEmit && npm run lint` → 0/0.
- [ ] **Step 5:** Commit:

```bash
git add "src/app/(dashboard)/articulos/page.tsx"
git commit -m "refactor(ux): articulos usa piezas de control comunes"
```

---

### Task 5: Aplicar piezas en `clientes/page.tsx`

**Files:** Modify `src/app/(dashboard)/clientes/page.tsx`

- [ ] **Step 1:** Importa las 4 piezas (igual que Task 4 Step 1).
- [ ] **Step 2: Sustituir 1:1** (lee el archivo):
  - `<Select>` OV/FAC → `<RecordTypeSelect value={tipo} onValueChange={setTipo} />`.
  - búsqueda `<div className="relative flex-1 min-w-[260px]">…</div>` → `<SearchInput value={search} onChange={setSearch} placeholder="Buscar cliente…" />`.
  - botón CSV → `<CsvButton onClick={exportCsv} disabled={visible.length === 0} />`.
  - celda vacía → `<TableCell colSpan={years.length + 3}><EmptyState message="Sin ventas en el rango seleccionado." /></TableCell>`.
- [ ] **Step 3:** Quitar imports sin usar (`Search`, `Download`, etc.); `npm run lint` 0 errores. (Mantén `Select*` si los selects de año siguen usándolos.)
- [ ] **Step 4:** `npx tsc --noEmit && npm run lint` → 0/0.
- [ ] **Step 5:** Commit:

```bash
git add "src/app/(dashboard)/clientes/page.tsx"
git commit -m "refactor(ux): clientes usa piezas de control comunes"
```

---

### Task 6: Aplicar piezas en `cliente-articulo/page.tsx`

**Files:** Modify `src/app/(dashboard)/cliente-articulo/page.tsx`

- [ ] **Step 1:** Importa las 4 piezas.
- [ ] **Step 2: Sustituir 1:1** (lee el archivo):
  - `<Select>` OV/FAC → `<RecordTypeSelect value={tipo} onValueChange={setTipo} />`.
  - búsqueda → `<SearchInput value={search} onChange={setSearch} placeholder="Buscar cliente, artículo o SKU…" />`.
  - botón CSV → `<CsvButton onClick={exportCsv} disabled={groups.length === 0} />`.
  - celda vacía `colSpan={9}` → `<TableCell colSpan={9}><EmptyState message="Sin ventas en el año seleccionado." /></TableCell>`.
- [ ] **Step 3:** Quitar imports sin usar; `npm run lint` 0 errores (mantén `Select*` para el select de año).
- [ ] **Step 4:** `npx tsc --noEmit && npm run lint` → 0/0.
- [ ] **Step 5:** Commit:

```bash
git add "src/app/(dashboard)/cliente-articulo/page.tsx"
git commit -m "refactor(ux): cliente-articulo usa piezas de control comunes"
```

---

### Task 7: Aplicar CsvButton + EmptyState en `tablas/page.tsx`

**Files:** Modify `src/app/(dashboard)/tablas/page.tsx`

- [ ] **Step 1:** Lee el archivo. Importa `CsvButton` y `EmptyState` de `@/components/common/...`.
- [ ] **Step 2: Sustituir** el botón de exportar CSV (`<Button … onClick={handleExportCSV}…>…CSV</Button>`) por `<CsvButton onClick={handleExportCSV} />`. Si hay un estado vacío con texto "—"/"Sin datos", envolverlo en `<EmptyState message="…" />`.
  - **No** cambies el selector de tipo de Tablas si su marcado/etiquetas no coinciden con `RecordTypeSelect` (usa `RECORD_TYPES`); déjalo tal cual.
- [ ] **Step 3:** Quitar imports sin usar (`Download`, `Button` si ya no se usa). `npm run lint` 0 errores.
- [ ] **Step 4:** `npx tsc --noEmit && npm run lint` → 0/0.
- [ ] **Step 5:** Commit:

```bash
git add "src/app/(dashboard)/tablas/page.tsx"
git commit -m "refactor(ux): tablas usa CsvButton/EmptyState comunes"
```

---

### Task 8: Build + push

- [ ] **Step 1:** `npm run typecheck && npm run lint && npm test && npm run build` → todo verde; en la salida de rutas **ya NO** aparecen `/kpis`, `/sales`, `/import`; sí `/subscriptions`.
- [ ] **Step 2:** Push:

```bash
git push origin main
```

---

## Notas
- Las piezas son presentacionales: el estado y los handlers siguen en cada página (sin cambios de comportamiento).
- `EmptyState` puede ser server/client (sin hooks); no necesita `"use client"`.
- Deploy en EasyPanel al terminar; verificar que el menú muestra Suscripciones y que /kpis,/sales,/import dan 404.
